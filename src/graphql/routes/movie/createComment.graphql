mutation createComment(
	$id: String!
	$title: String!
	$img_path: String!
	$comment: String!
) {
	createComment(
		id: $id
		title: $title
		img_path: $img_path
		comment: $comment
	) {
		id
		comment
	}
}
